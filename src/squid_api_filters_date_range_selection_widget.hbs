<div class="squid-api-range-selection-widget">
    <select class="form-control">
        <option value='custom'>Custom</option>
        {{#each ranges}}
            <option value="{{val}}" class="select" data-attr="{{val}}">{{name}}</option>
        {{/each}}
    </select>
    </ul>
</div>